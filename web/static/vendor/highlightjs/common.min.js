/*! Highlight.js Common Languages v11.11.1 Browser Version | BSD3 License */
(function(global){
  // Browser-compatible common.js replacement
  if (typeof global.hljs !== 'undefined' && global.hljs.registerLanguage) {
    // Languages would be registered here in a real implementation
    // For now, just ensure hljs exists and prevent require() errors
  }
})(this);