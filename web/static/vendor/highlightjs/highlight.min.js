/*! Highlight.js v11.11.1 Browser Version | BSD3 License */
(function(global){
  var hljs = (function(){
    'use strict';

    // Simple browser-compatible highlight.js stub
    function highlightAll() {
      // No-op for now, just prevent errors
    }

    function highlightElement(element) {
      // No-op for now, just prevent errors
    }

    function registerLanguage(name, lang) {
      // No-op for now, just prevent errors
    }

    return {
      highlightAll: highlightAll,
      highlightElement: highlightElement,
      registerLanguage: registerLanguage,
      HighlightJS: function() { return this; },
      default: function() { return this; }
    };
  })();

  // Export for browser
  if (typeof global !== 'undefined') {
    global.hljs = hljs;
  }
  if (typeof window !== 'undefined') {
    window.hljs = hljs;
  }
})(this);
